{
  "name": "ccm-infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy:staging": "cdk deploy --all --context env=staging",
    "deploy:prod": "cdk deploy --all --context env=production",
    "diff:staging": "cdk diff --all --context env=staging",
    "diff:prod": "cdk diff --all --context env=production",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "aws-cdk": "^2.170.0",
    "source-map-support": "^0.5.21",
    "typescript": "^5.6.0"
  }
}
